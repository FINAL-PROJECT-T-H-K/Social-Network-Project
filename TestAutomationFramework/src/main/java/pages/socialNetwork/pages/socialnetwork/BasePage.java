package pages.socialNetwork.pages.socialnetwork;

import org.openqa.selenium.WebDriver;

public class BasePage extends com.telerikacademy.testframework.pages.BasePage {
    public BasePage(WebDriver driver, String urlKey) {
        super(driver, urlKey);
    }
}
